# Section 1 input handoff

The upstream repository is:

https://github.com/romenmeitei/AISKG_01_Framework

Its deterministic snapshot run creates:

`Mushroom_KG_Reproducibility_Inputs_v2_from_upstream.zip`

The Section 2 notebook accepts this bridge ZIP through its manual-upload fallback.
The bridge must contain exactly one recursively discoverable `input_checksums.csv`.
For the manuscript snapshot, the fixed companion ZIP already present in this
repository remains the simplest one-click input.
